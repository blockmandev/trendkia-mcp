# Upper bound is load-bearing, not tidiness: mcp 2.x renamed FastMCP to MCPServer and changed the
# decorator API, so an unpinned install of this file today pulls 2.x and the server dies on import
# at `from mcp.server.fastmcp import FastMCP`. Production runs 1.27.2. Moving to 2.x is a real
# migration, not a version bump.
mcp>=1.2.0,<2
feedparser>=6.0.11
httpx>=0.27.0
uvicorn>=0.30.0
starlette>=0.37.0
